--- src/qt_gui_cpp_sip/CMakeLists.txt.orig	2020-07-24 15:59:00 UTC
+++ src/qt_gui_cpp_sip/CMakeLists.txt
@@ -50,6 +50,8 @@ if(sip_helper_FOUND)
     set(LIBQT_GUI_CPP_SIP_SUFFIX .so)
   elseif(WIN32)
     set(LIBQT_GUI_CPP_SIP_SUFFIX .pyd)
+  elseif(UNIX AND NOT ${PYTHON_VERSION} VERSION_LESS "3.8.7")
+    set(LIBQT_GUI_CPP_SIP_SUFFIX ".cpython-${PYTHON_VERSION_MAJOR}${PYTHON_VERSION_MINOR}.so")
   else()
     set(LIBQT_GUI_CPP_SIP_SUFFIX ${CMAKE_SHARED_LIBRARY_SUFFIX})
   endif()
