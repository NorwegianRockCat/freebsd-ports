--- src/CMakeLists.txt.orig	2021-02-11 19:56:10 UTC
+++ src/CMakeLists.txt
@@ -149,7 +149,9 @@ if (BUILD_SDF_TEST)
     )
   endif()
 
-  sdf_build_tests(${gtest_sources})
+  if (NOT BUILD_TESTING)
+    sdf_build_tests(${gtest_sources})
+  endif()
 
   if (NOT WIN32)
     set(SDF_BUILD_TESTS_EXTRA_EXE_SRCS Utils.cc)
