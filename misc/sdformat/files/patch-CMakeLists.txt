--- CMakeLists.txt.orig	2021-02-11 19:56:10 UTC
+++ CMakeLists.txt
@@ -4,7 +4,9 @@ if(COMMAND CMAKE_POLICY)
   CMAKE_POLICY(SET CMP0003 NEW)
   CMAKE_POLICY(SET CMP0004 NEW)
 endif(COMMAND CMAKE_POLICY)
-enable_testing()
+if (BUILD_TESTING)
+  enable_testing()
+endif()
 
 # with -fPIC
 if(UNIX AND NOT WIN32)
